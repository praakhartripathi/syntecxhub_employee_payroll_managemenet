# Database Configuration
# Update these values according to your MySQL setup

db.url=jdbc:mysql://localhost:3306/payroll_db
db.username=root
db.password=root
db.driver=com.mysql.cj.jdbc.Driver

# Application Configuration
app.name=Payroll Management System
app.version=1.0.0

# Payroll Configuration
payroll.pf.percentage=12.0
payroll.health.insurance=500.0

# Tax Slabs (India)
payroll.tax.slab1.limit=300000
payroll.tax.slab1.rate=0.05

payroll.tax.slab2.limit=500000
payroll.tax.slab2.rate=0.10

payroll.tax.slab3.rate=0.20
